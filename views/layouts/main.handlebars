<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/estilo.css">
    <title>Jokes</title>
</head>
<body>
    <header class="cabecalho">
        <h1>Jokes</h1>

        <nav>
            <a href="/"><button>Jokes</button></a>
            {{#if session.userID}}
                <a href="/jokes/dashboard"><button>Dashboard</button></a>
                <a href="/logout"><button>Sair</button></a>
            {{else}}
                <a href="/login"><button>Entrar</button></a>
                <a href="/registrar"><button>Registrar</button></a>
            {{/if}}
        </nav>
    </header>

    <section class="principal">
        {{{body}}}
    </section>

    <footer class="main-footer">
        <p><span>Jokes</span> &copy; 2025</p>
    </footer>
    
</body>
</html>